export function sum(baseValues: number[], multipler: number) {
  //creat a empty set to hold unqiue multiples
  const finalResult = new Set<number>();
  // base value is empty
  if (baseValues.length === 0) {
    return finalResult.add(0);
  }
  return 0;
}
