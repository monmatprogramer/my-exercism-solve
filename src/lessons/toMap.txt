// Learn to map parent child
Problem
const familyTree = {
      Khadija: ['Mateo'],
      Mateo: ['Rami'],
    }
const actual = degreesOfSeparation(familyTree, 'Khadija', 'Rami')

Solution result
Map(3) {
  'Khadija' => [ 'Mateo' ],
  'Mateo' => [ 'Rami' ],
  'Rami' => [ 'Mateo' ]
}


Parent tree: {A: [B, C, D]}
numbers of child has [B, C, D] = 3
{
A: [B, C, D]
B: [A, C, D]
C: [A, B, D]
D: [A, B, C]
}


Parent tree: {A : [B], B: [C]}
{
A: [B]
B: [A, C]
C: [A, B]
}


--------------------
// {}
parentGraph = new Map<string,string[]>();


// type of parenttree is Record<string, string[]>()
parentTree = {A: [B, C, D]}

//Convert parentTree to Map
// parentGraph = {A: [B, C, D]} 
parentGraph = new Map<string,string[]>(Object.entries(parentTree))


//step 1
// parentGraph = {A: [B, C, D]}  
// Get child from parent A
IF parentGraph.size() === 1:
    // parentKey = A
    parentKey = parentGraph.keys();
    // childLen = 3
    childLen = parentGraph.get().length()
    //childValue = [B,C,D]
    childValue = parentGraph.get();
    FOR 0 < childLen:  
        //i = 0 => tempValue = undefined  => tempValue = B ([C,D]) => parentGraph = {B: [A,C,D]}
        //i = 1 => tempValue = 8 => childValue = [B,C,D] => childValue = [C,B,D] => tempValue = C => parentGraph = {C: [A, B,D]}
        //i = 2 => tempValue = C => childValue = [D, B,C] => childValue = [D,B,C] => tempValue = D => parentGraph = {D: [A, B, C]}
      if(tempValue){
           childValue.unshift(tempValue);
           [childValue[0], childValue[i]]= [childValue[i], childValue[0]]
         }
        tempValue = childValue.shift();
        parentGraph.set(tempValue, [parentKey, childValue])

Else parentGraph.size() > 1:

